<local:ConflictViewBase x:Class="SkyScope.UI.ConfigFileListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SkyScope.UI">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ConflictViewResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Load order top-to-bottom. Click a file to see which others it conflicts with — green loses to it, red wins over it."
                   FontSize="10" Foreground="#8C9BB5" TextWrapping="Wrap" Margin="0,0,0,8"/>

        <!-- Search bar -->
        <Border Grid.Row="1" Background="#313744" CornerRadius="4" Margin="0,0,0,8" Padding="8,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#2E3440" CornerRadius="3" Margin="0,0,8,0">
                    <Grid>
                        <TextBox x:Name="SearchBox"
                                 Background="Transparent" BorderThickness="0"
                                 Foreground="#ECEFF4" CaretBrush="#ECEFF4"
                                 FontSize="11" Height="28" VerticalContentAlignment="Center"
                                 Padding="28,0,28,0"
                                 TextChanged="SearchBox_TextChanged"/>
                        <TextBlock Text="Filter by filename…" FontSize="11" Foreground="#4C566A"
                                   VerticalAlignment="Center" Margin="28,0,0,0"
                                   IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text="⌕" FontSize="15" Foreground="#4C566A"
                                   VerticalAlignment="Center" Margin="8,0,0,0"
                                   IsHitTestVisible="False"/>
                        <Button Style="{StaticResource ClearTextButton}"
                                local:TextBoxAssist.ClearTarget="{Binding ElementName=SearchBox}"/>
                    </Grid>
                </Border>

                <TextBlock x:Name="CountText" Grid.Column="1"
                           FontSize="10" Foreground="#8C9BB5"
                           VerticalAlignment="Center" Margin="0,0,2,0"/>
            </Grid>
        </Border>

        <!-- Empty state -->
        <TextBlock x:Name="EmptyText" Grid.Row="2"
                   Text="Run an analysis to populate this view."
                   FontSize="11" Foreground="#4C566A"
                   VerticalAlignment="Center" HorizontalAlignment="Center"
                   Visibility="Visible"/>

        <!-- File list (virtualized) -->
        <ItemsControl x:Name="FileList" Grid.Row="2" Style="{StaticResource VirtualizedNpcListStyle}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Tag="{Binding}" Click="FileRow_Click"
                            Style="{StaticResource ConflictHeaderButton}">
                        <Border CornerRadius="4" Margin="0,0,0,4" Padding="10,8">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#3B4252"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Highlight}" Value="Selected">
                                            <Setter Property="Background" Value="#4C566A"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Highlight}" Value="LosesToSelected">
                                            <Setter Property="Background" Value="{StaticResource DiffAddedBackground}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Highlight}" Value="BeatsSelected">
                                            <Setter Property="Background" Value="{StaticResource DiffRemovedBackground}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Background="#2E3440" CornerRadius="3"
                                        MinWidth="30" Height="20" Padding="4,0">
                                    <TextBlock Text="{Binding LoadOrderIndex}"
                                               FontSize="10" FontWeight="SemiBold" Foreground="#88C0D0"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <TextBlock Grid.Column="1" Text="{Binding RelativePath}"
                                           FontSize="11" Foreground="#ECEFF4"
                                           VerticalAlignment="Center" Margin="10,0"
                                           TextTrimming="CharacterEllipsis"/>

                                <Border Grid.Column="2" Background="#4C566A" CornerRadius="10"
                                        Padding="7,2" VerticalAlignment="Center"
                                        Visibility="{Binding HasConflicts, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="{Binding ConflictCount, StringFormat={}{0} conflict(s)}"
                                               FontSize="9" FontWeight="SemiBold" Foreground="#ECEFF4"/>
                                </Border>
                            </Grid>
                        </Border>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </Grid>
</local:ConflictViewBase>
